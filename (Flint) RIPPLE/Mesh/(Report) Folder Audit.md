---
id: 4d7d102f-6ee3-4fcd-aecd-b6135a4c7c07
title: "Folder Audit"
type: report
project: "RIPPLE"
tags:
  - "#report"
  - "#project"
  - "#ld/living"
  - "#stack/react"
  - "#status/dormant"
  - "#cluster/university"
status: dormant
created: "2026-08-17"
updated: "2026-08-17"
source_path: "/Users/brunojaamaa/Desktop/RIPPLE"
---

# Folder Audit

**Every folder in `/Users/brunojaamaa/Desktop/RIPPLE` is either documented below or listed as
excluded with a reason. Nothing is unaccounted for.** Read-only pass, 2026-08-17. Zero dataless
iCloud files found under the project root.

## Top-level folders

### `src/` 🟢

| | |
|---|---|
| Path | `/Users/brunojaamaa/Desktop/RIPPLE/src` |
| Purpose | The entire React client. Every route, component, hook and pure function |
| Files | **138** |
| Size | **792 KB** |
| Modified | 2026-03-22 to 2026-08-15 |

Subdirectories: `components/` (47), `lib/` (40), `hooks/` (29), `constants/` (8), `pages/` (8),
`stores/` (1), `types/` (1), `test/` (1), `workers/` (0).

Tests are **co-located**, not in a separate tree: 19 test files in `lib/`, 8 in `hooks/`, 6 in
`components/`, 3 in `constants/`. Total **34** files, **340** tests.

Top files: `App.tsx` (8 routes), `lib/litert.ts` (the LiteRT wrapper),
`lib/preprocessImage.ts` (Canvas, centre crop), `lib/classification.ts` (three confidence
tiers), `lib/offlineQueue.ts` (IndexedDB), `lib/councilDetection.ts` (Turf), `lib/supabase.ts`
(throws at module init without env), `lib/columnPrivileges.test.ts` (the ACL assertion).

⚫ **`src/workers/` is empty.** No `.gitkeep`, no files. Dead scaffolding from a plan that
expected a Web Worker for inference. LiteRT.js handles its own threading, so the directory was
never needed. Harmless, and worth deleting.

See [[(Note) The src Tree]].

### `supabase/` 🟡

| | |
|---|---|
| Path | `/Users/brunojaamaa/Desktop/RIPPLE/supabase` |
| Purpose | PostgreSQL schema, seed, test harness, and 6 Deno Edge Functions |
| Files | **49** |
| Size | **336 KB** |
| Modified | 2026-03-22 to 2026-08-15 |

`migrations/` holds **27** numbered SQL files. `seed/` holds **1**
(`001_melbourne_councils.sql`). `test/` holds **5** (`00_supabase_shim`, `01_verify`, `02_rls`,
`03_realtime`, `04_sprint21`). `functions/` holds **6** directories, each with one `index.ts`.
Plus `config.toml` and `storage.sql`.

⚫ **`supabase/.temp/`** holds 7 CLI cache files including `project-ref`, which names the
**deleted** project `nexccbcmziiltysfcmzi`. It is covered by `supabase/.gitignore`. The ref
grants nothing, since the project no longer exists.

🟡 **Verified locally, never hosted.** `pnpm verify:db` runs all 27 migrations and 102 assertions
against real PostgreSQL 17.11. Nothing here has run against the managed service. See
[[(Note) Data Model and RLS]].

### `scripts/` 🟢

| | |
|---|---|
| Path | `/Users/brunojaamaa/Desktop/RIPPLE/scripts` |
| Purpose | The verification harness and the build-time asset pipeline |
| Files | **9** |
| Size | **56 KB** |
| Modified | 2026-08-14 to 2026-08-15 |

`verify-db.sh` (bash, provisions PostgreSQL) · `verify-pwa.mjs` · `verify-mobile.mjs` ·
`verify-offline.mjs` · `verify-lighthouse.mjs` · `bench-inference.mjs` ·
`capture-screenshots.mjs` · `sync-litert-wasm.mjs` · `fetch-dev-model.mjs`.

This folder is the reason the README's numbers are checkable. See
[[(Note) The Verification Harness]].

### `public/` 🟡

| | |
|---|---|
| Path | `/Users/brunojaamaa/Desktop/RIPPLE/public` |
| Purpose | Static assets, the vendored WASM runtime, and the classifier model |
| Files | **17** |
| Size | **42 MB** |
| Modified | 2026-03-22 to 2026-08-15 |

Tracked: `favicon.svg`, `icons.svg`, `icons/icon-192.png`, `icons/icon-512.png`,
`icons/icon-512-maskable.png`, `offline.html`, `robots.txt`, `models/README.md`.

⚠️ **Gitignored and generated:** `litert-wasm/` (**8** files, four `.js` and four `.wasm`
variants: base, compat, jspi, threaded) and `models/ripple-classifier-v1.tflite`
(**5,434,517** bytes). Both are reproducible: `pnpm sync:wasm` and `pnpm fetch:model`.

This is why the folder is 42 MB while its tracked contents are trivial.

### `docs/` 🟢

| | |
|---|---|
| Path | `/Users/brunojaamaa/Desktop/RIPPLE/docs` |
| Purpose | Committed evidence and README screenshots |
| Files | **8** |
| Size | **1.8 MB** |
| Modified | 2026-08-15 |

`evidence/gates.json` (4 KB, every gate's measured result with the machine and date),
`evidence/lighthouse-summary.json`, and two full Lighthouse reports totalling **810 KB**.
`media/` holds 4 PNGs: `map`, `report`, `feed`, `offline`, generated by `pnpm capture:media`
from the production build in real WebKit at an iPhone 14 viewport.

Committing the full Lighthouse JSON rather than a summary is the right call and unusual. See
[[(Note) Media]].

### `.github/` 🟢

**1** file, `workflows/verify.yml`, **8 KB**. Three jobs, heavily commented, with two gates
deliberately excluded and the reason for each stated inline. See
[[(Note) Deploy and Environment]].

### `.claude/` ⚫ agent config

**2** files: `RESUME.md`, `settings.local.json`. Machine-local AI agent tooling, not project
code. Not opened.

Note that unlike PULSE, this project's `.claude/` is **not** gitignored, and there is no
`.codex/`, `.cursor/` or `.vscode/`. RIPPLE has a smaller agent-tooling footprint.

### `.vercel/` ⚫ deploy metadata

**2** files, gitignored. `project.json` holds the project id, org id and project name
(`ripple`). `README.txt` is Vercel boilerplate.

## Excluded, with reasons

| Folder | Reason for exclusion |
|---|---|
| `node_modules/` | Dependency tree. Reproducible from `pnpm-lock.yaml`. Ground rule 6 |
| `dist/` | Build output, **35** files, **51 MB**, mostly the copied WASM runtime and model. Reproducible via `pnpm build`. Ground rule 6 |
| `.git/` internals | Read-only git commands only, per ground rule 1 |
| `.env.local` | ⚠️ Secrets. Never opened. Ground rule 3 |
| `.env.test` | ⚠️ Committed on purpose. **Only its 8-line header was read**, to confirm it states it holds no secrets. It does |
| `.DS_Store` | macOS Finder metadata |

No `*silmu*`, `_secrets/`, `.next/`, `build/`, `coverage/`, `.cache/` or `.turbo/` directory
exists in this project. `pnpm coverage` would create one; none is present.

## Duplicate and dead flags

| Flag | Detail |
|---|---|
| ⚫ Dead | `src/workers/` is completely empty |
| ⚫ Stale | `supabase/.temp/project-ref` names a deleted Supabase project |
| 🟡 Legacy | `MASTERPLAN.md` OG1 says "**15** migrations", `docs/evidence/gates.json` says **334** tests. Both were true when written. Current: **27** and **340**. See [[(Note) Git History]] |
| 🟡 Stale section | `MASTERPLAN.md` OG3 reads as open. The deploy has since happened |
| ❓ Undefined | The `ml-models` storage bucket is referenced in `PRD.md` § 7.1 and **no migration defines it** |
| 🟡 Placeholder | `src/constants/terms.ts` carries **12** `[PLACEHOLDER]` markers |
| 🟡 Placeholder | `supabase/migrations/002_council_boundaries.sql` holds overlapping bounding boxes, not ABS polygons |

Notably absent: **no duplicate agent files.** Unlike PULSE, which carries byte-identical
`AGENTS.md` and `GEMINI.md`, RIPPLE has neither.

## Related

[[(Index) Complete File Inventory]] · [[(Note) The src Tree]] · [[(Report) Gaps & Questions]] ·
[[(Report) Project Summary]] · [[(Map) Master Map]]
